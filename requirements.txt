# ── RedRob dependencies ───────────────────────────────────────────────────────
# Core
numpy>=1.24,<2.0

# Bi-encoder (L2, L4) + reranking
sentence-transformers>=2.7.0
torch>=2.0.0            # CPU build is fine; install CPU wheel for ThinkBook

# Vector search (L4 accelerator, optional)
faiss-cpu>=1.7.4

# Cross-encoder polish (L7)
flashrank>=0.2.5

# NLP (L5 helper)
spacy>=3.7.0
# After install: python -m spacy download en_core_web_sm

# Language model perplexity (L5) — optional; heuristic proxy used if absent
# pip install https://github.com/kpu/kenlm/archive/master.zip
# (kenlm is intentionally NOT pinned here; see README for offline build note)

# PDF parsing (JD)
pymupdf>=1.23.0

# JD parsing (PROTOTYPE ONLY — pre-step, not used in offline rank.py)
google-generativeai>=0.5.0
groq>=1.5.0

# UI
streamlit>=1.32.0

# ONNX quantization (build_kb.py only, not at runtime)
onnx>=1.15.0
onnxruntime>=1.17.0
optimum[onnxruntime]>=1.17.0
